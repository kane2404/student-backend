# ⚠ DÉVELOPPEMENT UNIQUEMENT : ce mot de passe est public (il est dans Git).
# Pour tout environnement réel, ne commitez JAMAIS de Secret en clair : voir ../prod/secret.example.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: student-db-credentials
  labels:
    app.kubernetes.io/part-of: student-management
type: Opaque
stringData:
  POSTGRES_USER: students
  POSTGRES_PASSWORD: students
  POSTGRES_DB: students
